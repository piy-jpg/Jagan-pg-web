# Jagan PG - Database

This directory contains relational database schemas, seed migrations, and data access utilities for Jagan PG.

## 📁 Files
- `schema.sql` — Standard SQL schema for `rooms`, `enquiries`, `residents`, and `reviews`
- `seed.sql` — Default inventory and verified resident testimonials
- `db.js` — Data layer client adapter
